#!/usr/bin/env node

const app = require('..');
const config = require('../config');
const { logger } = require('../config/logger');

app.listen(config.express.port, () => {
  logger.info(`Express is running on ${config.express.host}${config.express.port}`);
});
